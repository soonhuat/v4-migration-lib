declare const constants: {
  did: string[]
  flags: string[]
  blob: string[]
  address: {
    zero: string
    one: string
    dummy: string
  }
  sign: {
    bytes32: {
      message: string
      signature: string
      publicKey: string
    }
  }
}
